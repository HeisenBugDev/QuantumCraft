package mods.QuantumCraft.research;

import java.util.ArrayList;
import java.util.List;

public class ResearchItemList {

	 /** Is the smallest column used to display a achievement on the GUI. */
    public static int minDisplayColumn = 500;

    /** Is the smallest row used to display a achievement on the GUI. */
    public static int minDisplayRow = 500;

    /** Is the biggest column used to display a achievement on the GUI. */
    public static int maxDisplayColumn = 500;

    /** Is the biggest row used to display a achievement on the GUI. */
    public static int maxDisplayRow = 500;

    /** Holds a list of all registered achievements. */
    public static List researchItemList = new ArrayList();

}
