 * Fix Capacitor GUI hover
 * debug handler not needed
 * Update gforge-instruction.md
 * Change dir for mcrun
 * move steam back to beta
 * improper array element
 * update changelog file. Preparing for release
 * crafting for capacitors! fixes #124!
 * add the capacitors to an arrayList so that they can be accessed later
 * inv name based off of multiplier
 * better power calculation
 * a little bit of balancing in the steam gen
 * updateAllLightTypes and markBlockForUpdate every 20 ticks. Added beacuse about 4 of the other machines have suffered from this problem and it stays in sync more than using updateNextTick.
 * steam texture! fixes #134
 * log the icon
 * set icons on texture stitch
 * fix machine casing texture
 * textures! fixes #113
 * nbt and get vars
 * gui
 * add some stuff for steam gen gui
 * change message
 * vars for progress bar
 * yay gui!
 * more base methods
 * y u still there?
 * bigger tank
 * better heat
 * fuel usage based off of heat?
 * Merge pull request #144 from HeisenBugDev/feature/142-Tiered-capacitors
 * Merge pull request #143 from HeisenBugDev/feature/22-fix-capacitor-enet-loop
 * names
 * names
 * warning
 * te max energy
 * load
 * move config ids
 * energy multiplier in block
 * fix #22 energy loop
 * switch to new handler
 * remove old debugHandler
 * debugHandler jar
 * logHandler
 * instructs
 * update isntructs
 * remove stats
 * remove println
 * don't post to hastebin if you don't want to be notified
 * add debug prompt
 * formatting
 * add debugPrompt var
 * better message
 * forge does this for us
 * whoeps!
 * call reset
 * reset the logs
 * refactor to debug handler
 * have both forge and qc logs
 * give the prompt info
 * jframe for prompting you
 * put log to hastebin
 * serverStop event
 * Update README.md
 * Well then, I forgot gfm
 * Add logo
 * only add in time if the second changes
 * closer to better debug
 * print to file
 * debug print for that
 * debug lines
 * fix nbt and array weirdness
 * move a simple fluid out of beta
 * just look at the diff and try not to get confused
 * move
 * nbt for steam gen
 * fix shift clicking
 * fixes #135
 * transferstackinslot is now magic!?!?!?!?!?
 * Finish 125-steam-gen Just gui and container left
 * decr stack
 * some stuff
 * fixes $140
 * reformat
 * delete the method
 * it's the easiest refactor in the world. I don't always refactor, but when I do, I make it simple. Stay clean my code.
 * refactor injector fixes #136
 * refactor extractor
 * refactor dematerializer
 * refactor for #140
 * refactor guiqdeenergizer
 * no need to override that
 * change to use the base and cast to ionforge
 * move this down to the base
 * more #137-ness and formatting
 * yum fancy 120 chars*
 * #137 for capacitor
 * reformat for java 120 line rather than the 80 line (oh derp)
 * refactor to new decrstack size in ion forge
 * one does not simply NOT write javadocs
 * 120 character formatting
 * refactor tileqdematerializer #137
 * more refactor
 * fixes #137
 * everything needs to call super!
 * format
 * if you call super it will break if it IS redstone powered fixes #133
 * gui id of 8 for steam generator
 * common proxy for steam generator
 * make public
 * add to client proxy
 * create gui "stub"
 * pointless because it's not reused
 * fill in the blanks
 * start container
 * that is more responsive
 * why two gui slots?
 * todo
 * super for debug
 * beta fluid
 * some concept
 * how can I spell derp badly enough to explain my derp?
 * register steam in loader
 * it's a gas!
 * yum code prettyness
 * forgot a null check
 * forgot a null check
 * drain
 * candrain
 * get info
 * register steam gen tTE
 * yum steak... er.. I mean steam
 * clean up
 * yum methods
 * add in the tank
 * create new TE
 * implement that!
 * implement base
 * fancy different changelog
 * remove unneeded method calls
 * load steam gen
 * steam generator config
 * add in block
 * empty class is empty
 * add railcraft api
 * debug instructions
 * convert to new debug fixes #41
 * convert to debughandler
 * we don't need that
 * convert to debug handler
 * remove println
 * call super for debug
 * debug
 * convert to debug handler
 * convert to debugprint
 * add methods
 * message
 * I'm thinking I could do better
 * start the debug handler
 * typo
 * yay warnings!
 * wans't needed fixes #123 <= awesome number
 * update instructions
 * go away emptyness
 * fail
 * wrapper
 * instructions
 * change gradle version
 * wrapper
 * add bc
 * why would  you ignore all jars?
 * yum libs
 * add abbrev
 * gradle
 * add gitignore
 * gitignore mcrun
 * gitignore
 * gradle
 * gradle
 * fixes #122
 * remove uid
 * refactor upgrade ID
 * todo
 * thou shall not beta any more
 * refactor capacitor
 * fix extract method
 * fix injecting
 * fix injecting power
 * fix
 * formatt!
 * eww bugs
 * watson note
 * refactor methods
 * Optimize extractor item dropping
 * Optimize dislocator item dropping
 * Optimize dematerializer item dropping
 * Optimize De-Energizer Item dropping
 * Optimize ION Forge item dropping
 * Merge branch 'origin/feature/22-Quantum-Capacitor'
 * Update item dropping in Injector
 * Item dropping in capacitor
 * add a constructor with ItemStack[] to SimpleInventory
 * change maxstacksize to 1
 * Attempt to add extraction
 * Rename the thing + add texture into GUI
 * Add new texture
 * Make it substract energy from buffer
 * Attempt to add charging to the capacitor (like injector)
 * Get out whitespace
 * Get rid of some more useless stuff
 * Get rid of some useless calls
 * Fix the dynamic request size
 * Dynamic request size
 * Merge branch 'origin/feature/22-Cool-GUI'
 * Finish the GUI
 * Edit the GUI code, still need to change numbers around
 * Add big powerbar texture
 * Merge branch 'origin/feature/22-Quantum-Capacitor'
 * fixes #117. client proxy
 * tile gui
 * common proxy
 * stub gui @sammko, please finish with a big power bar
 * empty container
 * todo
 * fixes #115
 * TE style power and block breaking thingy. Use multitool to retain power
 * location!
 * large amount of fixing stuff
 * some checks
 * OMG ENERGY!
 * remove those
 * moar work!
 * debug
 * oh god. stuff
 * register te
 * size
 * block
 * Revert "@sammko, no. We don't want that to happen for ALL the blocks, only itself"
 * @sammko, no. We don't want that to happen for ALL the blocks, only itself
 * trying to fix enet
 * block
 * tile
 * fix again
 * derp moment strikes back
 * load it
 * configure it
 * capicator
 * remove that
 * whoajfdk
 * EVYERHING!
 * remove
 * gitignore
 * watson!
 * watson!
 * merge
 * watson!
 * It would have been nice to change that
 * Theoretically? fix the self-source problem. No way to test ATM
 * fixed shift clicking in inject/extractor
 * -.- Fix shift clicking for ION forge
 * Fix shift clicking for stuff
 * Merge branch 'origin/feature/76-fancy-guis'
 * Finish dematerializer GUI
 * Add textures
 * Oh it only has one slot
 * Update demat container
 * Get out
 * E Key closing
 * I.O.N Forge GUI finshed
 * Add 4slot texture
 * Update I.O.N Forge container
 * Bluuuuuuur
 * Progress bar finished. very subtle but awesome :D
 * Add the texture for progress bar
 * Make that a little bit clearer
 * Stub GUI for deenergizer, TODO: add progrss bar
 * Update deenergizer container
 * shift clicks! fixes #62
 * moar shift clicks!
 * dematerializer shift clicking
 * shift clicking anyone?
 * Finish 0.6.0